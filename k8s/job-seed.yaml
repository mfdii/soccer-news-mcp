apiVersion: batch/v1
kind: Job
metadata:
  name: soccer-news-seed
  namespace: mcp-soccer-news
  labels:
    app: mcp-soccer-news
    job: seed
spec:
  template:
    metadata:
      labels:
        app: mcp-soccer-news
        job: seed
    spec:
      restartPolicy: OnFailure
      containers:
      - name: seed
        image: image-registry.openshift-image-registry.svc:5000/mcp-soccer-news/mcp-soccer-news:latest
        command: ["npm", "run", "seed"]
        envFrom:
        - secretRef:
            name: mcp-soccer-news-env
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
  backoffLimit: 3
